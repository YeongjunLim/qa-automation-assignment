# 문제4 — 함께 풀어나간 과정

> 상태: 아직 사용자와 함께 논의하지 않음 (설계/프로토타입은 완료 및 실행 검증된 상태). 이 문서에
> 논의 내용을 채워 넣을 예정입니다. 최종 산출물은 [README.md](README.md) 참고.

## 참고 — 이미 발생했던 실제 이슈 1건 (나중에 논의 시 다룰 내용)

프로토타입(`flaky_triage.py`)을 실제로 실행해 검증하는 과정에서, `build_prompt()`가
`str.format()`으로 프롬프트 템플릿을 채우다가 템플릿 안에 있는 JSON 스키마 예시의 중괄호
(`{ "category": ... }`)까지 자리표시자로 오인해 `KeyError`가 발생하는 버그를 발견했다.
`<<placeholder>>` 형태의 단순 문자열 치환으로 바꿔 해결했다. AI가 생성한 코드를 실행 검증 없이
제출하지 않고, 실제로 돌려봤기 때문에 발견할 수 있었던 문제라 기록해 둔다.

(나머지 진행 예정)
